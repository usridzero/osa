---
- hosts: all
  vars:
    ansible_python_interpreter: "/usr/bin/python"
  remote_user: vagrant
  roles:
    - common
  tags:
    - always

- hosts: aio
  vars:
    ansible_python_interpreter: "/usr/bin/python"
  remote_user: vagrant
  roles:
    - osa
      
